# Railway define PORT automaticamente — não precisa setar.
# PORT=3000

# Persistência das respostas entre deploys.
# 1) Crie um Volume no serviço Railway
# 2) Monte em /data
# 3) Defina:
DATA_DIR=/data

# Soft login da área /time (padrão: Matilha0108)
TEAM_PASSWORD=Matilha0108
# Opcional: segredo do cookie de sessão (padrão = TEAM_PASSWORD)
# AUTH_SECRET=um-segredo-longo
